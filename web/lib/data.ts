import config from "./site.config.json";

export const meta = config.meta;
export const caseStudies = config.work;
export const services = config.services;
export const contactData = config.contact;
export const socialLinks = config.social;
export const footerData = config.footer;

// About section data mapping
export const aboutData = config.about;
export const team = config.about.team;
export const serviceAreas = config.about.aside.serviceAreas;
